<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Accès réseau (pour brancher le backend plus tard). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Notifications (Android 13+ : autorisation à l'exécution). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Diffusion live (artiste) : caméra + micro, demandées à l'exécution. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:name=".DualMusicApp"
        android:label="Dual Music"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.DualMusic">

        <!--
          Désactive l'auto-initialisation de Sentry (son SentryInitProvider s'exécute AVANT
          notre Application et planterait sans DSN : « DSN is required »). L'init se fait
          exclusivement dans DualMusicApp (no-op sûr tant que le DSN est vide).
        -->
        <meta-data android:name="io.sentry.auto-init" android:value="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|keyboard|uiMode|density|fontScale|locale|layoutDirection"
            android:theme="@style/Theme.DualMusic">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Service Firebase Cloud Messaging (réception des push). -->
        <service
            android:name=".DualMusicMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>
</manifest>
